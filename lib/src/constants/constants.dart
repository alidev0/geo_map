/// urlTemplate
const urlTemplate =
    'https://api.mapbox.com/styles/v1/{mapUser}/{mapStyleId}/tiles/256/{z}/{x}/{y}@2x?access_token={mapAccessToken}';

/// zoomRef
const zoomRef = 3.0;

/// sizeRef
const sizeRef = 192.0;

/// scaleRef
const scaleRef = 1.0;
